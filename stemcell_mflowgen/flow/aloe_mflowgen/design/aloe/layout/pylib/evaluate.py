from __future__ import division
from expr import read_expr

def eval_bias_gen_bk(individual, expr_file_path)
	expr_dic = read_expr(stage, expr_file_path)
	c0 = abs(float(dict_netlength_expr['VIP'])-float(expr_dic['VIN']))
	c1 = abs(float(dict_netlength_expr['von1'])-float(expr_dic['vop1']))
	c2 = abs(float(dict_netlength_expr['VCP'])-float(expr_dic['VCN']))
	c3 = abs(float(dict_netlength_expr['VOP'])-float(expr_dic['VON']))
